/** Wrapper so grid/flex children stretch controls to the full cell width. */
export const formFieldClass = "form-field";

/** Shared control styling for input, select, and textarea (type-independent width). */
export const formControlClass = "form-control";

export const formLabelClass = "form-label";
