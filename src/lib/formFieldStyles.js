/** Single-line fields: input, select, searchable triggers, country/phone row. */
export const formControlClass = "form-control";

/** Multi-line fields (not forced to h-10). */
export const formTextareaClass = "form-control form-control--textarea";
